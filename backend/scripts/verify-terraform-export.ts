import { strict as assert } from "node:assert"; import { buildDeterministicZip } from "../src/terraform-export/zip-builder";
const files = [{ path: "main.tf", content: Buffer.from("terraform {}") }, { path: "README.md", content: Buffer.from("safe") }]; const first = buildDeterministicZip(files); const second = buildDeterministicZip([...files].reverse()); assert(first.equals(second)); assert.equal(first.readUInt32LE(0), 0x04034b50); assert(first.includes(Buffer.from("main.tf"))); assert(first.includes(Buffer.from("terraform {}"))); console.log("Terraform ZIP verification passed");
