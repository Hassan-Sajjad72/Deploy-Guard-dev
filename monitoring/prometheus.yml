global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: deployguard-aws-runtime
    metrics_path: /api/monitoring/metrics
    authorization:
      type: Bearer
      credentials: deployguard-local-monitoring
    static_configs:
      - targets:
          - host.docker.internal:5000
